apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: featureflag-logs-pvc
  namespace: cahit
spec:
  storageClassName: ""   # PV ile eşleşmesi için boş (manual binding)
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: featureflag-logs-pv 